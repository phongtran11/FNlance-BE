export enum EUserRoles {
  ADMIN = 'admin',
  ADVERTISER = 'advertiser',
  USER = 'user',
}
export enum EUserMajor {
  FRONTEND = 'frontend',
  BACKEND = 'backend',
  DESIGNER = 'designer',
  OTHER = 'other',
}
